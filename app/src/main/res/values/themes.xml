<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlacementFinder" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/royalBlue</item>
        <item name="android:iconTint" tools:targetApi="o">@color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="Theme.PlacementFinder" parent="Base.Theme.PlacementFinder" />
    <style name="Base.Theme.PlacementFinder.NoActionBar" parent="Theme.Material3.Light.NoActionBar" >
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>